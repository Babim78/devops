server {
        listen 80;
        listen [::]:80;

        server_name {{ local }}

        root /var/www/html/{{ local }};
        index index.html;

        location / {
                try_files $uri $uri/ =404;
                }
       }
